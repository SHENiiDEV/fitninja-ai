# /etc/nginx/sites-available/fitninja.conf
# Enable site: sudo ln -s /etc/nginx/sites-available/fitninja.conf /etc/nginx/sites-enabled/

server {
    listen 80;
    listen [::]:80;
    server_name fitninja.example.com; # 👈 Change to your actual domain or IP address

    root /var/www/customAI/fitninja-ai/public; # 👈 Your VPS project path
    index index.php index.html;

    charset utf-8;

    # Gzip Compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    # Pass PHP scripts to FastCGI server (PHP 8.4 FPM)
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.4-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    # Deny access to hidden files (.env, .git, etc.)
    location ~ /\.(?!well-known).* {
        deny all;
    }
}
